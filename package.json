{
  "name": "aws-serverless-lambda-typescript-lambda-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "tsc && serverless deploy function --function capitalOneLex",
    "start": "tsc && serverless invoke local --function helloWorld",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.0.22"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.62",
    "axios": "^0.20.0"
  }
}
